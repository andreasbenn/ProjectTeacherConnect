export default class Course {
    constructor (id, fullName, email, course,){
        this._id = id;
        this.fullName = fullName;
        this.email = email;
        this.course = course;
        }
}