export default class Topic {
    constructor (id, topicName, course, popCounter,){
        this._id = id;
        this.topicName = topicName;
        this.course = course;
        this.popCounter = popCounter;
    }
};
